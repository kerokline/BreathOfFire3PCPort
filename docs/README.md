# docs/

Project-owned notes. Agents and humans both put findings here.

## The documents that exist

| Doc | What it is |
|---|---|
| [`PLAN.md`](PLAN.md) | The scoping document. Target analysis, architecture options and the reasoning that selected one, the phased path, constraints. |
| [`STATUS.md`](STATUS.md) | Where the project is right now: order of work, open decisions, outstanding obligations. Start here. |
| [`HANDOFF.md`](HANDOFF.md) | What the next session picks up, how, and the traps already paid for. Rewritten, not appended to. |
| [`IDEAS.md`](IDEAS.md) | Intake for unscheduled proposals, each with a feasibility rating and a first step. |
| [`DIVERGENCE.md`](DIVERGENCE.md) | The ledger of intentional behavioural changes. Read before changing game behaviour; append when you do. |
| [`THIRD_PARTY.md`](THIRD_PARTY.md) | The permissive third-party code the repo carries, with each notice in full: today the SatPixie CRT shader (MIT / public domain) behind the `satpixie` look, DIV-0043. |
| [`LICENSING.md`](LICENSING.md) | Why the repo is licensed the way it is, and the constraints that follow from wanting a commercial handoff to be possible. Read before vendoring anything or relaxing rule 1. |
| [`SHARED_SOURCE.md`](SHARED_SOURCE.md) | Why the two binaries are compilations of one source tree, what that licenses, and the catalogue of changes the *porting house* made. Read before treating a PSX finding as a PC fact. |
| [`DAT_CONTAINER.md`](DAT_CONTAINER.md) | The `DAT/*.DAT` format: a chunk stream mirroring the PSX `.EMI` section list, unencrypted. Audio re-encoded to WAV, overlay code dropped, most other sections byte-identical to the JP disc. |
| [`media-stack-survey.md`](media-stack-survey.md) | What the port uses to draw, play and decode, and which of it still exists on Windows 11. DirectDraw + `IDirect3D3`, DirectSound 1, DirectInput 3, MCI/VFW for FMV, a statically-linked MP3 decoder. One thing is actually broken: the Indeo 5 logo video. |
| [`replacing-mci.md`](replacing-mci.md) | What replacing the MCI/VFW FMV path with a bundled decoder would take. Anatomy of `Fmv_Play` `0x59E360` — one function, two call sites, modal and blocking — plus the codec licensing table. Costed, not scheduled (`IDEAS.md` I7). |
| [`SCAFFOLDING.md`](SCAFFOLDING.md) | Phase 0: the launcher, the five-byte detour, the `BOF3X_ORIGINAL` A/B switch, and how `symbols.toml` generates the one binding of every name. **Read before taking over a function** — §3 is the procedure. |
| [`asset-loading-path.md`](asset-loading-path.md) | The 16-slot file layer and `LoadDatFile`'s four chunk kinds, read from the exe. Kind-0 chunks land in one arena at `0x803580 + tag` — the port's repacked image of PSX RAM. In progress. |
| [`launcher-settings.md`](launcher-settings.md) | The launcher's settings dialog: why a bare Win32 dialog resource and no toolkit, the three places a setting can go (our environment variables, the game's own `BOF3.CFG`, nowhere yet), and the measurement that shows the disc's `START.EXE` is an autorun shell and `SETUP.EXE` is InstallShield 5 - neither is a configurator. |
| [`windowed-mode.md`](windowed-mode.md) | The port has a built-in windowed mode: a two-line `BOF3.CFG`, or F8 at runtime. Not a patch. Also skips the FMV exclusive mode-set. Superseded in part by [`window-modes.md`](window-modes.md) (DIV-0032..0034); describes Capcom's window, which runs only under `BOF3X_ORIGINAL`. |
| [`save-interchange.md`](save-interchange.md) | The PC save is the PSX `0x10B0` game block from offset 0 — same checksum, same offsets — with the character name widened 5→9 bytes. `tools/save_convert.py` converts both ways; converted PSX saves load and play on PC (owner, 2026-09-19); PC→PSX is checked statically only. |
| [`crash-reporter.md`](crash-reporter.md) | Always-on fault logging in the injected DLL (`CRASH n:` lines in `bof3x.log`, a minidump beside it) and `tools/crash_report.py` to read dumps, WER's included. Observes, never handles. |
| [`known-defects.md`](known-defects.md) | What the 2001 port does wrong on a current machine, as observed: who saw it, in which configuration, what is established about the cause. Observations, not decisions to fix. |
| [`call-trace.md`](call-trace.md) | The in-process call tracer: which functions a run reaches, counts and edges, a per-frame call hash identical across launches and passing original-vs-ours, the frame-skip finding, and the takeover work queue. |
| [`attract-remaining.md`](attract-remaining.md) | Stage 1's remainder: every function the attract sequence runs that is not ours - 541, grouped, counted, with callers - and the blind spot found making it: `pe_funcs.py` misses every function reached only through a pointer, some 7,300 of them, 89 in the attract run. Also the WndProc correction (`0x4FC6F0`, not `0x4FC6A0`). In progress. |
| [`remaining-catalog.md`](remaining-catalog.md) | The other axis: every function not ours - 9,241 of 10,246 starts - labelled by the part of the game it belongs to (PSX overlay pairs, address ranges, host, neighbour), with reach measured per route for every start (the routes replayed under the hidden entry list, 2026-09-25: 442 route-reached functions are not ours, 407 of them pointer-reached and invisible to the all-calls traces - round eight's queue), what each label rests on, and the shape it gives the later waves: the field menu recipe, a boss fight, the spell effects and area handlers as class harnesses, the scenario banks last. Also the A/B-side glyph-guard crash found on the way. `tools/remaining_catalog.py`, `analysis/trace_hidden_recipes.sh`. In progress. |
| [`cut-content.md`](cut-content.md) | What the disc holds and play never shows, from TCRF's page (summarised, cited): the owner's list (skills, music, narration, text, the whelp's portrait, Sunder's animation to loop), and every unused and enemy-only skill located by row, overlay and PC entry - which confirms `magic.toml`'s names are one id off. Draft. |
| [`takeover-queue-round8.md`](takeover-queue-round8.md) | The eighth round's queue: the 442 functions the recorded routes enter that are still Capcom's - 407 of them pointer-reached and invisible to the all-calls traces - in two waves of 22 groups, A the combat route's 236 (`CA`..`CM`), B the shop's and the world map's 206 (`DA`..`DI`); what is different about pointer-reached and jumped-to entries, the live check per wave. Queued 2026-09-25, not yet cut. |
| [`takeover-queue-round9.md`](takeover-queue-round9.md) | The ninth round: the routes re-traced after round eight - the "76" hidden entries are 29 distinct, and of them only the task scheduler and two task bodies are game logic (group EA); the rest are switch cases, retired set-up callbacks and the MP3 decoder. The spell round's size (~2,070 functions behind `Magic_Rows`) and its harness group SH. Started 2026-09-25. |
| [`battle_phases.md`](battle_phases.md) | Group CA of the eighth round: the battle frame and the phase table's first three phases: the start, command input, the commit; 18 functions ours, 139 of 140 controls refused. In progress. |
| [`battle_actions.md`](battle_actions.md) | Group CB of the eighth round: the action phases under `0x42F220`: the kind, ability, item, effect and after steps, seven tables; 23 functions ours, 117 of 118 controls refused. In progress. |
| [`battle_turn_steps.md`](battle_turn_steps.md) | Group CC of the eighth round: the round's end and the battle's end: the extra round, the status chain, the win, the write-back; 20 functions ours, 109 of 109 controls refused. In progress. |
| [`battle_result.md`](battle_result.md) | Group CD of the eighth round: the battle result (the PSX `BATL_END.EMI`): EXP, level-ups, zenny, drops and their window; 17 functions ours, 69 of 72 controls refused. In progress. |
| [`battle_fx_tasks.md`](battle_fx_tasks.md) | Group CE of the eighth round: the battle effect tasks: the damage popup, the pose task, the follower, the magic-effect dispatch; 19 functions ours, 81 of 81 controls refused. In progress. |
| [`enemy_ai_ops.md`](enemy_ai_ops.md) | Group CF of the eighth round: an enemy object's state 0: the entrance, the idle loop, the hit, the death; 20 tables named; 22 functions ours, 112 of 112 controls refused. In progress. |
| [`battle_obj_states.md`](battle_obj_states.md) | Group CG of the eighth round: the battle objects' states through `0x64DFE0`: swing, cast, hit, end of action; 22 functions ours, 114 of 115 controls refused. In progress. |
| [`battle_actor_copies.md`](battle_actor_copies.md) | Group CH of the eighth round: not actor copies but the command's target choice and the battle item window; 16 functions ours, 90 of 92 controls refused. In progress. |
| [`battle_menu_states.md`](battle_menu_states.md) | Group CI of the eighth round: the attack and item commands' menu states; 15 functions ours, 74 of 74 controls refused. In progress. |
| [`magic_fx_reached.md`](magic_fx_reached.md) | Group CJ of the eighth round: the spell effects the fight casts: a disc-and-fan effect, Pilfer (DIV-0046 kept), the Healing Herb, the backdrop dim; 25 functions ours, 78 of 78 controls refused. In progress. |
| [`battle_odds.md`](battle_odds.md) | Group CK of the eighth round: ten small ones: banner timers, two effect handlers, the steal double's and the sparkles' phases; 10 functions ours, 48 of 49 controls refused. In progress. |
| [`battle_win_states.md`](battle_win_states.md) | Group CL of the eighth round: the battle windows under record handler 3: party row, command cross, label, banner, gauges; 19 functions ours, 40 of 41 controls refused. In progress. |
| [`window_kinds.md`](window_kinds.md) | Group CM of the eighth round: the window-kind handlers: the HP gauge states, the banner and message windows; 12 functions ours, 44 of 44 controls refused. In progress. |
| [`worldmap_area.md`](worldmap_area.md) | Group DA of the eighth round: the world map's area code: area inits and handlers, the place plate (a sprite animation), the HUD task, three effect kinds; 30 functions ours, 97 of 97 controls refused. In progress. |
| [`mode_states.md`](mode_states.md) | Group DB of the eighth round: top-level modes 6 and 7, `MsgBox_SystemChoice` (D22: faithful through id 0x90, aborts past it), the 8 px UI draw, the field core's fades, scenario chapter 1; 20 functions ours, 104 of 107 controls refused. In progress. |
| [`event_leader.md`](event_leader.md) | Group DC of the eighth round: the event script's leader: the step, the swap, the menu, the encounter, a passage's contents; the way-blocked tests; 27 functions ours, 94 of 95 controls refused. In progress. |
| [`task_sched.md`](task_sched.md) | Group EA of the ninth round: the task scheduler - `Task_RunAll` and the rest of its hand-written unit (`0x5A98A0..0x5A9A21`), the esp switch every task body stands on; the register contract a task sees; the call tracer's frame count kept on `0x5A98A0`; `0x576CD0` / `0x577B80` are switch cases, not task bodies; 8 entries ours, 30 of 33 controls refused by count, 3 by fault. In progress. |
| [`map_field_objects.md`](map_field_objects.md) | Group DD of the eighth round: map-cell and area set-up handlers, three menu draws (not field objects), the event script's skip-switch and `8x` ops; 16 functions ours, 132 of 132 controls refused. In progress. |
| [`field_hidden.md`](field_hidden.md) | Group DE of the eighth round: members' states, the party-set actions, the sprite set helpers, the encounter placement's driver; 15 functions ours, 81 of 83 controls refused. In progress. |
| [`shop_states.md`](shop_states.md) | Group DF of the eighth round: the shop overlay's first table: the mode, the inn, the night, the save menu shared with the save point; 25 functions ours, 104 of 104 controls refused. In progress. |
| [`shop_states2.md`](shop_states2.md) | Group DG of the eighth round: the shop overlay's second table: buy and sell; task 0's title loop; 24 functions ours, 116 of 121 controls refused. In progress. |
| [`menu_lists.md`](menu_lists.md) | Group DH of the eighth round: the field menu's states and the list draws under record handler 6 (DIV-0041 kept); 20 functions ours, 56 of 56 controls refused. In progress. |
| [`menu_draw_helpers.md`](menu_draw_helpers.md) | Group DI of the eighth round: the shop and battle-menu panel draws and their slides under record handlers 7 and 8 (DIV-0041 kept); 25 functions ours, 39 of 39 controls refused. In progress. |
| [`input-script.md`](input-script.md) | Scripted input: recipes of pad presses played inside the game in its own frames (`BOF3X_INPUT`, `tools/input_run.py`), and routes the owner records (`BOF3X_RECORD`, §5a) - the shop, the world map, a combat, each playing back identically. The game writes its own captures at each `shot` (`BOF3X_SHOT_DIR`, F11; `--no-front` leaves the window alone, 2026-09-24). Also the pad's bit layout, the frame counter, and the field menu button being save data. Working. |
| [`movement-script.md`](movement-script.md) | The field objects' movement script: the interpreter (`0x576B50`, PSX `0x801A9D38`), its `0xF0` command group, the attach command `F8 07` that sets the handle of known-defects D6, and where the scripts live - compiled into `BOF3.exe`'s `.data` behind the area descriptor table `0x667590`. `tools/movement_scan.py`. Draft. |
| [`field-frame.md`](field-frame.md) | The field's frame loop: the per-frame entries `0x517200` / `0x517240`, the members' frame, the 30-object and 4-party-object loops into the kind table, the party objects' screen update and projection `0x57B830` / `0x57B860`, and the overlay list's sort and draw `0x592F00`. Twelve functions ours, fuzzed with 63 negative controls. In progress. |
| [`kind2-object.md`](kind2-object.md) | The kind-2 object's script runner `0x573080` (probably the field camera's focus - a hypothesis): its two back-to-back jump tables and five states, and the ground's slope `AreaMap_Slope` `0x5722D0`. Eight functions ours; the glide's division by zero and the unmasked corner index written down. In progress. |
| [`mode-tasks.md`](mode-tasks.md) | The title task `0x4621C0` (task 1's body) and its start check, sprite, backdrop and logo, and the field's mode-2 handler `GameMode_Field` `0x4959F0`. Six functions ours; the port's own changes against the PSX title listed. In progress. |
| [`object-kinds.md`](object-kinds.md) | The field objects' kind handlers: the table `0x65F5F8` by the sprite's pose byte (wander, approach, avoid, wait, still, fades, turn), the pace table `0x65F5DC`, the loops' talk / touch / step-aside / area-handler calls. Twenty functions ours, fuzzed with 41 negative controls; ten unreached by the attract cycle. In progress. |
| [`field-modes.md`](field-modes.md) | Group A of the second parallel round: the scenario dispatcher `Field_ModeDispatch` and its two tail dispatchers, the chapter call thunk, scenario 16's eleven handlers (the attract demo) and the CLUT strip fade and restore; 17 functions ours, 37 controls refused; D8, the fade's red/blue swap. Through `ab22`. In progress. |
| [`field-event.md`](field-event.md) | Group B: the event script's field side - the party leader's frame and first two states, the area set-up that places the party, a member joining, the party-set loader; 31 functions ours, 46 of 48 controls refused by count; D10, the 8-bit zone counter. Through `ab22`. In progress. |
| [`event-script.md`](event-script.md) | Group C: the event script interpreter - the op dispatch, if, switch and their skips, the attract cycle's object placements, the bit-flag helpers; 28 functions ours, 45 of 46 controls refused; all 200 areas' placement scripts decode byte-identical to the PSX disc's (`tools/event_scan.py`); D9. Through `ab22`. In progress. |
| [`frame-callees.md`](frame-callees.md) | Group D: the field frame loop's unread callees - the effect runner, the camera turn (effect kind `0x13`) and its three states, the slot records, the tint frame, the members' screen updates; 8 functions ours, 49 of 51 controls refused; D11, D13. Through `ab22`. In progress. |
| [`title-states.md`](title-states.md) | Group E: the eight handlers of `Title_States`, the title's music helpers, the demo's corner badge, the logo's draw sets, and the scenario start the demo goes through; 17 functions ours, 40 controls refused. Through `ab22`. In progress. |
| [`field-blocked.md`](field-blocked.md) | Group G: the blocked-ahead test - `Field_ObjectBlockedAhead`, `Field_ObjectBlocked`, the map test under them and the object tests they share; 9 functions ours, 28 controls refused. Through `ab22`. In progress. |
| [`takeover-queue-round3.md`](takeover-queue-round3.md) | The third round's queue after groups H and J: K (the top-level task flow), L (the movement commands), M (sprite animation); the round's 137 functions through `ab24`, 466 ours. Done. |
| [`msgbox.md`](msgbox.md) | Group H of the third round: the dialogue box - `Msg_OpenScript`, the eight-state frame task, `MsgBox_Step`, and the last two text pens `Text_DrawAt` / `Text_EmitGlyph`; 43 functions ours, 48 of 51 controls refused by count (one by fault); the DIV-0006 note; what the catalogue had wrong. Through `ab24`. In progress. |
| [`window-task.md`](window-task.md) | Group J: the window/task layer - window records, their three kinds and five states, `Window_DrawFrame` - and the area change above it; 27 functions ours. Through `ab24`. In progress. |
| [`mode-flow.md`](mode-flow.md) | Group K: the top-level task flow - boot, the field task and its first two modes, `Area_Enter` `0x594E60`, all 21 transitions; 42 functions ours (the queue said 18), 94 of 95 controls refused; D16; `ab24` found one fix, `Boot_Task`'s call shape. In progress. |
| [`move-cmds.md`](move-cmds.md) | Group L: the movement commands and the party's moves - `Party_MoveMember`, the `MoveCmd_*` handlers, `MoveScript_FindLabel` and the script lookups; 15 functions ours, 74 of 76 controls refused; D14, D15. Through `ab24`. In progress. |
| [`sprite-pose.md`](sprite-pose.md) | Group M: sprite animation, facing and the effect pool - `Sprite_SetAnimation*`, the frame-upload queue, `Sprite_FaceDirection`, `Effect_Release` / `Effect_ClearAll`; 10 functions ours. Through `ab24`. In progress. |
| [`takeover-queue-round4.md`](takeover-queue-round4.md) | The fourth round: N (the glyph draw and the owner's display fixes), O (the area entry), P (queued as the title's hidden cluster, found to be the field menu's item use), Q (sound), R (the Direct3D draw); 101 functions, 567 ours; `ab25`, captures `ab25b`. Done. |
| [`glyph-draw.md`](glyph-draw.md) | Group N: `D3d_DrawGlyph` `0x5A2900` and `Font_GlyphTexture`, the vertex-block fuzz group R reuses, and three of the owner's display fixes - DIV-0025 (texel centres, D17), DIV-0026 (the Config controller panel), DIV-0027 (the Yes / No chooser), all confirmed in game 2026-09-23; 2 functions ours; D18. In progress. |
| [`area-entry.md`](area-entry.md) | Group O: the area's link list and the drop-in party placement - `Area_LinkAt` `0x5951D0`, `Party_DropIn` `0x531F90` and three callees; 5 functions ours, 37 of 40 controls refused. Fuzz only - no recorded route reaches them. In progress. |
| [`item-use.md`](item-use.md) | Group P: the field menu's item effects - the 33 pointer-reached handlers in `0x496AD0`'s range the queue took for title code, the Faerie Tiara, the message box's commits, the system choice, the pad auto-repeat; 59 functions ours, 70 of 72 controls refused by count; D21..D23. Fuzz only - no recorded route reaches them. In progress. |
| [`sound.md`](sound.md) | Group Q: the sound layer - `Sound_PlayEffect`, `Music_Play` and the fades, `Sound_Tick` and the frame wait, the DirectSound layer under them; 23 functions ours, 64 of 70 controls refused by count; D24..D26 (D26, the fades, fixed as DIV-0028). Through `ab25`. In progress. |
| [`d3d-draw.md`](d3d-draw.md) | Group R: the Direct3D draw - the ordering-table walk `Gfx_DrawOTag`, the dispatch `0x59EE50`, its handlers and helpers, the cell-texture lookup; 12 functions ours on N's vertex-block fuzz; D27, D28. Through `ab25` / `ab25b`. In progress. |
| [`takeover-queue-round5.md`](takeover-queue-round5.md) | The fifth round: the last 24 functions the attract sequence reaches, as S, T and U - 27 functions, 594 ours - leaving only the DIV-0010 sprite copies, closed in round six. Through `ab26`. Done. |
| [`display-env.md`](display-env.md) | Group S: the PSX library's display calls over DirectDraw - the `PutDispEnv` / `PutDrawEnv` shapes, the present `0x59EDF0`, the clear - and `Snd_LoadBank` `0x587CD0`; 8 functions ours, fuzzed on a fake surface. Through `ab26`. In progress. |
| [`tex-page.md`](tex-page.md) | Group T: the page textures `D3d_BuildPageTexture` / `D3d_RefreshPageTexture`, their pixel converters and the DirectDraw surface helpers, with the fake DirectDraw `src/game/ddraw_fuzz.*`; 8 functions ours; D30. Through `ab26`. In progress. |
| [`tex-cells.md`](tex-cells.md) | Group U: the glyph and cell textures, their unpackers and helpers, on T's fake DirectDraw; 11 functions ours; D31, D32. Through `ab26`. In progress. |
| [`takeover-queue-round6.md`](takeover-queue-round6.md) | The sixth round: the 164 functions the owner's recorded shop route reaches, in eight groups (V1, V2, W, X, Y, Z, M, D); 789 ours; `ab26` - the shop A/B 35 of 35. Done. |
| [`event-ops.md`](event-ops.md) | Group V1: the leader's walk, its button tests, the event script's shop and scene ops `EventOp_5x` / `Dx` / `9x` and its last placements; 35 functions ours (32 queued, 3 helpers), 84 of 86 controls refused; a clang miscompile the fuzz caught (§8). Through `ab26`. In progress. |
| [`event-objs.md`](event-objs.md) | Group V2: the event script's object ops - the jump, the actor timers and the equipment tick, the cells; 22 functions ours, 46 of 50 controls refused; D33. Through `ab26`. In progress. |
| [`char-stats.md`](char-stats.md) | Group W: `Char_RecalcStats`, the equipment previews, `Inventory_Add` with the inventory and the item tables, `Menu_DrawHand`; 20 functions ours, 54 of 55 controls refused by count; D34..D36. Through `ab26`. In progress. |
| [`save-menu.md`](save-menu.md) | Group X: save, load, the inn, the shop's prices and the sound stream - `Save_ReadFile`, `Save_ListFiles`, `Save_ReadSummaries`, `Sound_LoadStream`; 44 functions ours, 61 of 63 controls refused; D38. Through `ab26`. In progress. |
| [`menu-windows.md`](menu-windows.md) | Group Y: the menu and shop windows - `Msg_OpenSystem`, `Menu_YesNo`, `Menu_DrawButtonRow`, `Menu_DrawPiece`, `Text_CharCount` and the rest; 37 functions ours, 49 of 50 controls refused; the patch sites of DIV-0018, DIV-0027 and DIV-0030 inside; D39, D40. Through `ab26`. In progress. |
| [`member-sprites.md`](member-sprites.md) | Group Z: the second and third members' frame and states, the follow step and its helpers, the cell-ahead tests the leader shares; 21 functions ours, 46 of 48 controls refused. Through `ab26`. In progress. |
| [`field-misc.md`](field-misc.md) | Group M of the sixth round: map patches, the move tests `MoveCmd_TestFB` / `TestFC`, three Direct3D handlers and four PSX setters; 16 functions ours, 70 of 77 controls refused by comparison. Through `ab26`. In progress. |
| [`sprt-draw.md`](sprt-draw.md) | Group D: the three Direct3D sprite handlers `D3d_DrawSprt` / `8` / `16`, reimplemented with DIV-0010 inside and switchable - the last attract-reached function not ours; 3 functions ours, 32 controls refused; D37. Through `ab26`. In progress. |
| [`battle_setup.md`](battle_setup.md) | Group BA of the seventh round: the battle set-up chain - the per-turn steps (status expiry, wake-ups, HP drift, AP upkeep), the faster-side test, the opening message, the queued item return; 19 functions ours, 80 of 81 controls refused; D48. In progress. |
| [`battle_flow.md`](battle_flow.md) | Group BB: the battle task and the turn flow - the phase dispatch, the task pool, the enemies' run and script ticks, defeat and drops, item and ability magic starts, the rolling digits; 21 functions ours, 91 controls refused; D47. In progress. |
| [`battle_windows.md`](battle_windows.md) | Group BC: the battle windows under the window task - party status, the command cross and icons, the message and target boxes, the enemy status - and the party/object state runners; 22 functions ours, 50 of 53 controls refused; D46; where the Chinese command banner is drawn. In progress. |
| [`battle_window_draw.md`](battle_window_draw.md) | Group BD: the battle windows' draw helpers - numbers, bars, tiles, lines, cells - and the skill list's can-use, AP-cost, flag and ability-list helpers; 19 functions ours, 60 of 62 controls refused; D44, D45. In progress. |
| [`battle_damage.md`](battle_damage.md) | Group BE: damage, effects and affinities - the turn order builders, Battle_ApplyDamage, Battle_CalcDamage (the formula in words), Battle_ScaleDamage, Effect_ApplyResult, the element and holy affinities, the enemy AI's action choice; 18 functions ours, 99 controls refused; D49..D51. In progress. |
| [`battle_misc.md`](battle_misc.md) | Group BF: 27 small battle helpers - actor bits and contexts, the banner queue, the item-menu set-up, the enemy AI turn check, a string copy; 90 of 92 controls refused. In progress. |
| [`battle_sprites.md`](battle_sprites.md) | Group BG: battle target flags and popups, the formation stat mods, the encounter set-up (enemy data copy, the enemy-name windows, boss encounters), the tint and CLUT map; 32 functions ours, 70 controls refused; D54, D55; the enemy name's data path for the localisation work. In progress. |
| [`battle_items.md`](battle_items.md) | Group BH: not a menu but the Healing Herb's sparkle effect and the shared magic-effect draws, the battle actor helpers, four PSX primitive setters and the sound stream; 24 functions ours, 71 of 72 controls refused by count; D52, D53. In progress. |
| [`inventory_ops.md`](inventory_ops.md) | Group BI: not inventory but the random encounter's placement on the field map - party and enemy spots, cell fit, reachability, the on-screen test, the camera - and the battle intro's party steps; 25 functions ours, 76 of 80 controls refused; D56, D57. In progress. |
| [`battle_draw.md`](battle_draw.md) | Group BJ: three Gouraud Direct3D handlers (PolyG3, LineG2, LineG3), the battle item and skill list draws, a CLUT tint; 6 functions ours, 66 controls; D43. In progress. |
| [`takeover-queue-round7.md`](takeover-queue-round7.md) | The seventh round's queue: the 209 functions the owner's combat route reaches and the attract, shop and world-map routes do not - the battle engine in ten parallel groups. In progress. |
| [`world-map.md`](world-map.md) | The owner's recorded world-map route: its A/B against Capcom's device, the compass needle the port never drew (D41, DIV-0044), the 40-function queue for the next wave in the owner's order, and the place plates as paint for the localisation build. In progress. |
| [`world-map-hud.md`](world-map-hud.md) | Group 1 of the world-map wave: the map task's frame state machine, the frame (dial, keyboard legend, needle), the sprite draw, the HUD and two map-view helpers - seven functions ours, read against the map overlay off the JP disc, fuzzed with 47 controls; the dial's opacity read as a port change (D42), the place plates' rectangles placed in the area's `0x800D3800` section by a JP / US diff, eleven copies of the map code found. In progress. |
| [`map-layers.md`](map-layers.md) | The map and draw layers under the field's frame: `AreaMap_Frame` `0x56E6C0` (the view's follow, scroll and camera), the view's rebuild `MapView_Build` `0x56EC00` and its textures `0x56F9B0`, the wall handler `0x571500` (`MapCell_Handlers` `0x3F..0x4C`), the area header's pass `0x571AF0` and its palette cycle `0x571B40`. Six functions ours, each fuzzed alone with recording stand-ins. In progress. |
| [`area-backdrop.md`](area-backdrop.md) | The area's backdrop: the sky gradient `AreaMap_DrawBackdrop` `0x571BE0` (entry 1 of `AreaMap_EntryHandlers`, widened to the 426-column view under DIV-0041), its siblings `AreaMap_TextureCycle` `0x571D30` and `AreaMap_SlotZones` `0x571E20`, and `WorldMap_PinSprite` `0x4112A0` - "the sky" of the widescreen queue that turned out to be a centred sprite. Four functions ours, each fuzzed alone with recording stand-ins, 37 negative controls. In progress. |
| [`map-scroll.md`](map-scroll.md) | The map view's cell ring: the four scroll shifts `0x56E9A0` / `0x56EA30` / `0x56EAB0` / `0x56EB50`, the run placement `MapView_PlaceRuns` `0x571FF0`, and the view's set-up for an area `Field_ViewReset` `0x56F670` with its three callees - the baked texture patches `0x56FAD0`, the set-up entry pass `0x571720` and the palette cycles' first frame `0x5717B0`. Nine functions ours, each fuzzed alone against a clone with recording stand-ins, 50 negative controls; the PSX twins read instruction for instruction. In progress. |
| [`sprite-draw-order.md`](sprite-draw-order.md) | The field's sprite structures, found by working the takeover queue: **30 + 4 sprite objects of `0xA4` bytes at `0x7DEE80` / `0x802000`**, the 40-entry draw list and the pass `0x593060` that orders it over 0x37 layers, the 56 x 28 view-cell grid, the draw-item index pool, the animation script format. Twenty-two functions ours, the pass `Sprite_DrawPass` among them - fuzzed with recording stand-ins for the callees that are not - all live-checked. In progress. |
| [`psx-library-layer.md`](psx-library-layer.md) | The porting house wrote Sony's libraries rather than rewrite the game's calls into them: libgpu setters and ordering-table links, `getTPage` / `getClut`, a sine, and a GTE whose registers are globals at `0x7DE4xx` with a 20-deep matrix stack. 62 functions ours - `ApplyMatrix` at 2.6 M calls, and the x87 ones in `double` once the control word was measured at `0x027F` on 11 M live calls; the transforms over them; a `NormalColor` that discards its lighting. Also where `-fno-strict-aliasing` was found necessary, and why the matrix product waits on the owner. In progress. |
| [`dialogue-localisation.md`](dialogue-localisation.md) | Stage 2, the text swap, as built: per-language overlay `DAT`s made locally from the player's disc by `tools/loc_build.py` - dialogue, system pools, item and ability names, the US font doubled into the port's glyph table - and the engine side, DIV-0005..0009. Measured: the text code is the glyph index; English is monospaced at 8 px; the three pens that advance text; why the system pool moved; `name[16]` against the US disc's 12. §6 is the open list; §8 the strings that live in the exe - one chunk kind each, DIV-0015..0020 - and how to find the next. In progress. |
| [`USER_CHECKS.md`](USER_CHECKS.md) | Checks only the owner can do, in game. Agents add; the owner ticks and reports. |
| [`save-files.md`](save-files.md) | Saves are `BISLPS0?.DAT`, 4,784 bytes, sixteen slots; the four save-path functions; and the defect of a fresh save vanishing from the reopened menu, fixed as DIV-0002 (the clean A/B under `BOF3X_ORIGINAL` still owed). In progress. |
| [`display-overhaul.md`](display-overhaul.md) | The UI overhaul plan (2026-09-23, proposed): window modes without an exclusive mode-set, integer scaling, a shader present pass, and the widescreen brainstorm. The finding that shapes it: the render path is ours from the ordering table to the `Blt`; only the set-up `0x5A5160`, WinMain and the FMV are not, so a Direct3D 11 backend of our own is a backend swap. Lists the ten Capcom functions that hold the widescreen cull sites, and that the PSP release is a native MIPS build worth mining for how Capcom did 16:9. |
| [`render-backend.md`](render-backend.md) | The Direct3D 11 render backend behind DirectX 6's own objects: the seven object slots hold our DirectDraw- and Direct3D-shaped objects, a frame is recorded on whatever stack the game draws from and replayed on a fiber under the present, into a target at an integer scale. One function taken over (`Display_Setup` `0x5A5160`, DIV-0031); the draw handlers it replays have since been taken over by their own groups ([`d3d-draw.md`](d3d-draw.md), [`sprt-draw.md`](sprt-draw.md), [`battle_draw.md`](battle_draw.md)). Since: the wide target (DIV-0041), the scale from the window (DIV-0042), the looks (DIV-0037, DIV-0043), the depth-0 corner clamp (DIV-0044). Step 2 of the display overhaul. In progress. |
| [`window-modes.md`](window-modes.md) | Step 3 of the display overhaul: WinMain `0x4FCB00`, WndProc `0x4FC6F0` and `Fmv_Play` `0x59E360` ours (2026-09-23). A resizable window or a borderless one the size of the monitor with no mode-set (DIV-0032), the game running while unfocused with the pads zeroed (DIV-0033, I12), the frame debt clamped (DIV-0034), the FMVs into the window at an integer scale (DIV-0035). The loop read call for call; the batch `wm1`. In progress. |
| [`cheats.md`](cheats.md) | The launcher's Cheats dialog (DIV-0045, DIV-0046): EXP and zenny multipliers applied in our `Battle_EnemyDefeated`, and the sibling's steal patch on the port's two copies of the steal roll, found by their rate table. Confirmed in game on the combat route; the zenny line off the results screen still owed. |
| [`crt-look.md`](crt-look.md) | The CRT look (DIV-0037): scanlines and halation in the present, our own shaders built into the dll - the owner chose a pre-packaged look over a slang loader, and the model shader is GPL, so nothing of it is vendored. `BOF3X_PRESENT=crt`, tuned by `BOF3X_CRT`. In progress. |
| [`widescreen.md`](widescreen.md) | The widescreen plan: 426 x 240 with no crop (the owner), the PSP's primitive shift (+53), then the culls - three of which 53 columns outrun - the full-frame fills and the edge-anchored UI; a survey build behind `BOF3X_WIDE=1` first. Planned. |
| [`display-setup.md`](display-setup.md) | The DirectDraw / Direct3D set-up `0x5A5160` read to its last instruction (2,645 bytes to `0x5A5BB4`, not `pc_funcs`' 3,722): `IDirectDraw4` + `IDirect3D3` + a HAL-only `IDirect3DDevice3`, the windowed and fullscreen surface paths, every state it sets, every global it writes and who reads each (ours or Capcom's). `Cfg_RenderMode` is the device-record index - 0 is "Software Render" - and `D3d_ScaleX/Y` are mode / 320 and / 240. The helpers `0x5A5130`, `0x5A6690` (a device-name lookup, not a re-init), `0x5A3CC0`, the teardown `0x5A6380`, the enumeration callbacks; and `D3d_AfterDraw` `0x59F580`, which reads the back buffer back into a texture that nothing in `.text` then draws. In progress. |
| [`psp-widescreen.md`](psp-widescreen.md) | What the PSP release's `BOOT.BIN` does for 480 x 272, read from the ELF (2026-09-23): a 384 x 240 logical frame with every primitive's x shifted by 32, presented as its 384 x 216 window at row 12 scaled 1.25x - widened by 64 columns, cropped by 24 rows, one uniform scale. Draw environment, projection centre and perspective unchanged; the terrain and area-map culls widened, the message box's side placements moved to keep their edge distance, full-frame fills re-authored; no runtime aspect option. Every changed constant tabled for `display-overhaul.md` §4d, with `tools/psp_elf.py`. Draft. |
| [`controls.md`](controls.md) | The input path read whole: DirectInput 7's first joystick mapped digitally with a fixed button order, the 32-entry keyboard table from `BOF3.CFG` lines 3+, and the Config panel's Controller row - the PlayStation's own button swap, save data. **Done 2026-09-24:** the pad through SDL3 (DIV-0050), bindings in `bof3x.ini` and the launcher's Controls dialog with press-to-bind and pad navigation, the Controller row's one PlayStation icon column (DIV-0051, D58) - all confirmed by the owner; the in-game binding screen deferred to I20. |
| [`config-screen.md`](config-screen.md) | The in-game Config screen's text, which is in `BOF3.exe` and in no `DAT`: six label operands, seventeen 16-byte option records, six controller names, and the two row tables that are byte-identical to the PlayStation's. Translated from the player's disc as DIV-0015, with the 8 px UI font `0x516E70` read, the donor's 8 x 8 set imported for it (DIV-0016) and the selected row in the dialogue font (DIV-0017). Confirmed in game. §8: the buttons above the panel, which are the whole menu's verb table (DIV-0018). |
| [`menu-screens.md`](menu-screens.md) | The field menu: mode 3, the state machine at `0x589970` and its state block `0x929F00`, mapped from the owner's walk under read-only sampling; and the four defects of the 2001 menu the owner reported, with the cause of two. In progress. |
| [`title-menu.md`](title-menu.md) | The start screen's three rows are artwork, not text: a 4bpp page in `START.DAT` drawn by `0x5888D0` with its row widths as immediates. How `BOF3X_LANG=en` rebuilds it from the player's disc - NEW GAME and LOAD GAME as the disc has them, CONFIG cut from their letters (DIV-0014). Confirmed in game by the owner, 2026-09-21. |
| [`attract-mode.md`](attract-mode.md) | The attract sequence exists on PC and runs the real engine. Also: the game's **four-coroutine task system** (hand-written `esp` swap, 16 KB stacks), and why `Rand` is deterministic — seed 1, no `srand` in the binary, no clock reachable from game logic. Groundwork for a regression oracle. In progress. |
| [`kinship-probe-text-engine.md`](kinship-probe-text-engine.md) | PLAN §8 step 2, the load-bearing experiment. **Passed** 2026-09-18: PSX names transfer onto the PC binary, and global blocks keep their internal layout at a per-block constant delta. |
| [`new-code-audit.md`](new-code-audit.md) | What the 2026-09-25 readability audit of the code new to the game could not settle without the game: eight bugs confirmed by reading (a surface slot reused under a pending draw, the Japanese overlay's F9 lines, the 0 key hidden in Controls, a recording's lost last run, ...), ten unchecked reports, six open questions, each with how to check it in game. In progress. |
| [`lifter-feasibility.md`](lifter-feasibility.md) | IDEAS I3, the crude x86→C lifter, studied with a prototype (`tools/lift/`) on a synthetic corpus: every function lifted to 64-bit-clean C and matched against the original bytes in an emulator, negative controls refused. Instruction semantics are the easy part; the blockers are the task system's raw `esp` swap, the x87 control word off the game thread, discovery and the imports. Draft; not yet run on `BOF3.exe`. |
| [`bsim-evaluation.md`](bsim-evaluation.md) | Ghidra BSim measured against our own hand-verified pairs. Works cross-ISA (4 of 8 at rank 1, one perfect match), but it is a seed generator, not an oracle — and it produced one confident wrong answer. |
| [`overlay-transfer-feasibility.md`](overlay-transfer-feasibility.md) | What the PSX overlay name corpus is actually worth: 121 named overlay functions, not 29,036, and why indexing them into BSim must be done on a copy. |
| [`kinship-probe-battle-engine.md`](kinship-probe-battle-engine.md) | The successor probe, on a subsystem with no pre-existing landmarks. **Passed** 2026-09-18: the transfer scales, PSX *overlay* functions transfer, and value-sequence search on constant tables is a second anchor that needs no seed. |

Plus [`prior-art/`](prior-art/) — notes on four projects that have already hit
walls we are walking toward (OpenRCT2, devilution/DevilutionX, TR1X, Diaphora).
Start at its [`README`](prior-art/README.md): the cross-cutting findings are
worth more than any single note, and one of them (nobody kept a regression
oracle, all of them paid) bears directly on [`DIVERGENCE.md`](DIVERGENCE.md).

Outside `docs/`, two artifacts came out of that probe:
[`../symbols.toml`](../symbols.toml), the PC-side symbol map (same shape as the
sibling's, plus `psx` and `status` fields), and [`../tools/`](../tools) —
`pe_funcs.py`, `pe_disasm.py`, `pe_xref.py`. The tools have grown since into
some thirty-five: `gen_symbols.py`, which turns `symbols.toml` into the C++
header the code in [`../src/`](../src) builds against; the harness
(`input_run.py`, `calltrace.py`, `mem_dump.py`, the probes); the data tools
(`dat.py`, `loc_build.py`, `save_convert.py`, `psx_disc.py`); and the pairing
and reading tools (`psx_pair.py`, `pe_hidden.py`, `event_scan.py`,
`movement_scan.py`). Each doc names the ones it used. Their output goes to
`analysis/`, which is gitignored because it is derived from copyrighted game
code.

Four documents, four questions — keep them from bleeding into each other:
`PLAN.md` says what we intend and why; `STATUS.md` says what is true today;
`HANDOFF.md` says what to do next; `IDEAS.md` holds what nobody has committed
to. **When `PLAN.md` and `STATUS.md` disagree, `STATUS.md` is right and
`PLAN.md` needs updating.**

## Naming

- `SCREAMING_CASE.md` for durable subsystem documents — `TEXT_ENGINE.md`,
  `BATTLE_RAM.md`, `SYMBOLS.md`. These are the ones people come back to.
- `kebab-case.md` for a single investigation, incident, or experiment —
  `matcher-first-results.md`, `fullscreen-fallback-bug.md`. Dated by content,
  not by filename.

## Status header

Every document opens with one:

```
**Status:** DRAFT | IN PROGRESS | STABLE | SUPERSEDED (by <doc>)
```

Add the date it was last verified, not the date it was written. A `STABLE`
document with a two-year-old verification date is telling you something useful.

## The evidence rule

**A claim about the binary cites the measurement that produced it.**

Not "the game dispatches through function-pointer tables" but "recursive descent
from `0x5BA057` reaches 309 functions; 738 indirect calls are reg-based memory
operands (full `.text` sweep, capstone, 2026-09-18)". Include the command or
script where one exists.

This is inherited from the sibling repo and it survives the archival/living
split intact. A living project still needs to know what is *true* — it just gets
to decide what to do about it. The two questions stay separately answerable
(`CLAUDE.md` rule 6).

Where a claim rests on the PC port agreeing with the PSX version, say which one
was actually checked. The sibling's
[`PC_PORT_CROSS_REFERENCE.md`](../../BreathOfFire3Recomp/docs/PC_PORT_CROSS_REFERENCE.md)
is the worked example of doing this well, including two cases where the port was
right and the PSX-side notes were wrong.

## Evidence tiers for names

When recording what a function or field *is*, tier the claim explicitly —
`evidence`, `hypothesis`, or `unnamed`. The sibling repo's `NAME_MAP.md` imposes
this discipline and it is the reason its name corpus is trustworthy.
A name transferred automatically by the matcher (PLAN §3) is a **hypothesis**
until something confirms it.

## What does not go here

- Game data, or anything extracted from it. Including inline in a document —
  `.gitignore` cannot catch a paste. (`CLAUDE.md` rule 1.)
- Player-facing changelogs. `DIVERGENCE.md` is an engineering record, not release
  notes; they serve different readers and should not be merged.
