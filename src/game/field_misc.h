// Group M of the sixth takeover round: the map's narrow blocked test and its
// slope wrapper, the area patch list's entry and the draw-item lookup under it,
// the flat overlay of a map cell, the area palette's tint, the slope lookup with
// the height scale, the two move-script tile tests (ops FB / FC), three Direct3D
// draw handlers (POLY_F4, POLY_G4, LINE_F3) and four libgpu setters.
// docs/field-misc.md.
#pragma once

void FieldMisc_Inject();
