// Group CK of the eighth takeover round: the banner pool's two kind handlers,
// two of the effect handlers under Effect_ApplyResult, and six small states of
// the battle's effect objects reached through call tables built on the stack.
// docs/battle_odds.md.
#pragma once

void BattleOdds_Inject();
