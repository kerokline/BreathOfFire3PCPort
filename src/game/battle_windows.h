// The battle windows: the party row's status bars, the command cross, the
// command label and target banners, the plain boxes, the window task's small
// helpers and the battle party's state step (the seventh round's group BC,
// docs/battle_windows.md).
#pragma once

void BattleWindows_Inject();
