// The event script's three placements still Capcom's after round five (5x, 9x,
// Dx), the party leader's walk - its sub-state 1, the step's target, the six
// button tests Field_LeaderWalk makes first and what they call - and the
// chapter / area step hooks the step consults. 36 functions.
// docs/event-ops.md.
#pragma once

void EventOps_Inject();
