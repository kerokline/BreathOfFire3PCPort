// The movement commands and the party's moves: the movement script's callees
// that were still Capcom's - the party member's move and the arctangent under
// it, the kind-2 object's move and placement, op F7's timed move, the attach
// command F8 with its detach and its move to the handle's object, the label
// search, the script variables, the object kind, the frame-script peek and
// the pose wait - and the demo's party reset, chapter 16's call-table entry 0.
// docs/move-cmds.md.
#pragma once

void MoveCmds_Inject();
